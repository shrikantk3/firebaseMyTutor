import { UserFullNameDirective } from './user-full-name.directive';

describe('UserFullNameDirective', () => {
  it('should create an instance', () => {
    const directive = new UserFullNameDirective();
    expect(directive).toBeTruthy();
  });
});
